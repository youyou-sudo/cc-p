services:
  proxy:
    build: .
    ports:
      - "${PROXY_PORT:-3050}:3050"
    env_file:
      - path: .env
        required: false
    environment:
      PORT: "3050"
      HOST: "0.0.0.0"
    restart: unless-stopped